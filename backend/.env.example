# Server Configuration
PORT=5000
NODE_ENV=production

# Database Connection (MySQL)
# Example Local: mysql://root:password@localhost:3306/blog_webapp
# Example Cloud (Aiven / TiDB / Railway): mysql://avnadmin:secret@mysql-service.aivencloud.com:15432/defaultdb?ssl-mode=REQUIRED
DATABASE_URL="mysql://root:root@localhost:3306/blog_webapp"

# JWT Secret for Session & Tokens
JWT_SECRET=super_secret_vichaar_jwt_key_2026_change_this_in_production

# Frontend Client URL(s) for CORS (comma-separated if multiple)
CLIENT_URL=http://localhost:3000,https://your-frontend.vercel.app
